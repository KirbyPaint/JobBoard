@using JobBoard.Models;
<h1>Welcome to the Job Board!</h1>
@if (Model.Count == 0)
{
  <p>There are no job listings available</p>
}
<ul>
  @foreach (JobOpening item in Model)
  {
    <li>@item.Title</li>
    <li>@item.Description</li>
  }

  @* @foreach (ContactInfo item in Model)
  {
    <li>@item.Name</li>
    <li>@item.Email</li>
    <li>@item.PhoneNumber</li>
  } *@
</ul>
<a href='/items/new'>Add a new job</a>
<a href='/items'>See list of jobs</a>